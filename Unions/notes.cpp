/*

*Structure
    -Sturcture is defined by struct keyword
    -Size of structure is sum total of all individual elements inside it.
    -Structure is a collection of user defined data Types stored at different memory locations
    -not memory efficient

*Union
    -Union is defined by union keyword
    -Union is of the same size as the greatest element inside it.
    -Union is a colletion of user defined data types stored at same memory location
    -Memory efficient because it uses the same memory location for different purposes

*Enumeration
    -Enums is defined by enum keyword
    -Size of enum is 4 x (number of items constants inside) as each constant is an integer of 4 bytes
    -Enum is a collection of user defined data types for representing integer based constants
*/